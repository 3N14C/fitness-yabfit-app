import { FC } from "react";
import { View } from "react-native";
import { TypesTrain } from "./types-train";

export const Programs: FC = () => {
  return (
    <View style={{ marginTop: 40 }}>
      <TypesTrain />
    </View>
  );
};
